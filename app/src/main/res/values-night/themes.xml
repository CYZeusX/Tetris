<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GAME" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:windowBackground">@color/mainBackgroundLight</item>
        <item name="materialButtonStyle">@style/NoThemeButton</item>
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.GAME" parent="Base.Theme.GAME" />
</resources>